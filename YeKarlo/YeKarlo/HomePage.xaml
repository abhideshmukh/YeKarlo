<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="YeKarlo.HomePage" Padding="20">
  <ScrollView>
    <StackLayout  Orientation="Vertical" >
      <Button x:Name="btn" Text="getdata"/>
      <ListView x:Name="lView" HasUnevenRows="True">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height ="Auto"/>
                      <RowDefinition Height ="Auto"/>
                      <RowDefinition Height ="Auto"/>
                    </Grid.RowDefinitions>
                    <!--<Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>-->
                    <Frame Grid.RowSpan="3">
                      <Image x:Name="creep" Source="{Binding ImageUrl}" Grid.Row="0"/>
                      <Label Text="{Binding Title}" Grid.Row="1"/>
                      <Label Text="{Binding Description}" Grid.Row="2" />
                    </Frame>
                  </Grid>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ScrollView>
</ContentPage>